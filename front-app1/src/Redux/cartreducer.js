import * as actionTypes from "../Redux/actionTypes";

const initalState = {
    cart: [],
    TotalPrice: 0
};

const Cartreducer = (state = initalState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}